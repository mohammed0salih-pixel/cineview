# المرحلة 3: Git + CI/CD (جاهزة للتنفيذ)

> ملاحظة: هذا الملف داخل docs ليتوافق مع بيئة VS Code الحالية.

## 🎯 الهدف

- كل تعديل محسوب ومراجع.
- نشر تلقائي بعد نجاح الاختبارات.
- سجل زمني موثق لكل فكرة (مهم للبراءات).

---

## 1) استراتيجية الفروع (Branching Strategy)

**مقترح عملي وبسيط (مناسب للمراحل المبكرة):**

- `main`: مستقر وقابل للنشر دائمًا.
- `develop`: دمج الميزات قبل النقل إلى `main`.
- `feature/*`: لكل ميزة جديدة.
- `fix/*`: للإصلاحات السريعة.

**سياسة الدمج:**

- لا دمج مباشر إلى `main`.
- دمج عبر Pull Request مع مراجعة واحدة على الأقل.
- كل PR يجب أن ينجح فيه CI (اختبارات + lint).

---

## 2) اختبارات تلقائية عند كل Push

**GitHub Actions (اقتراح ملف جاهز):**

- عند كل `push` أو `pull_request`:
  - تشغيل `npm test`
  - تشغيل `npm run lint`
  - تشغيل `npm run build`

**ملاحظة:** الملف الفعلي يجب أن يكون في:

```
.github/workflows/ci.yml
```

---

## 3) نشر تلقائي بعد النجاح (Auto Deploy)

**مسارات شائعة:**

- Vercel
- Netlify
- GitHub Pages
- VPS/Server

**سلوك مقترح:**

- عند نجاح الـ CI على فرع `main` → يتم النشر تلقائيًا.

---

## ✅ سجل زمني قانوني (مهم للبراءة)

**إرشادات الالتزام القانونية:**

- كتابة رسائل Commit واضحة ومؤرخة.
- ربط كل فكرة بوثيقة أو Issue.
- استخدام Tags للإصدارات المهمة.

**صيغة رسائل Commit الموصى بها:**

```
feat: add analysis cache for determinism
fix: prevent null input in decision engine
chore: update lint config
```

---

## المطلوب منك الآن

إذا رغبت بتنفيذ المرحلة 3 عمليًا (داخل المشروع البرمجي نفسه):

1. افتح جذر المشروع الحقيقي (الذي يحتوي package.json).
2. سأضيف ملفات GitHub Actions تلقائيًا.
3. سأجهز Template للـ PR وملف سياسة الفروع.

---

### جاهز للتنفيذ فور توفر جذر المشروع

- CI Pipeline كامل
- Auto deploy حسب المنصة التي تختارها
- توثيق كامل لسجل التطور
